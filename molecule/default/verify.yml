# vim: set ft=yaml ts=2 expandtab:
---

- name: Verify
  hosts: all,!infrastructure_group

  tasks:

  - name: ssh login is possible
    delegate_to: ci-proxy
    command: ssh -i /tmp/id_rsa -o StrictHostKeyChecking=no testssh1@{{ inventory_hostname }} /usr/bin/true
    changed_when: false

...
